spring:
  datasource:
    url: jdbc:h2:mem:billing_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: none

billing:
  security:
    allowed-origins:
      - http://127.0.0.1:5173

logging:
  file:
    name:
